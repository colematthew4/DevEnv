name: Test Installation
on: [push]
jobs:
  install-apps:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - run: REPOS_DIR=$PWD ./apps/install.zsh
      - run: cat $HOME/.zshrc
